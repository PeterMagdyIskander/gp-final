import { connect } from "react-redux";
const ReportMenu = () => {
  return <div className="container-centered">
    <div>
      take info from user
    </div>
  </div>;
};
export default connect()(ReportMenu);
